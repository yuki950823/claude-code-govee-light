# runtime artifacts (auto-managed, machine-specific)
generation
animator.pid

# your lamp's LAN address (IP + MAC) — generated by `-Discover`, kept out of the
# public repo. See govee-config.example.json for the shape.
govee-config.json
